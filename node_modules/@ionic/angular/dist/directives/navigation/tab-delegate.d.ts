import { ComponentFactoryResolver, ElementRef, Injector } from '@angular/core';
import { AngularDelegate } from '../../providers/angular-delegate';
export declare class TabDelegate {
    private elementRef;
    constructor(elementRef: ElementRef, cfr: ComponentFactoryResolver, injector: Injector, angularDelegate: AngularDelegate);
    onNavChanged(): Promise<void>;
}
