import { Directive, ElementRef, HostListener, Optional } from '@angular/core';
import { Router } from '@angular/router';
export class TabsDelegate {
    constructor(router, elementRef) {
        this.router = router;
        if (router) {
            elementRef.nativeElement.useRouter = true;
        }
    }
    onTabbarClick(ev) {
        const tabElm = ev.detail;
        if (this.router && tabElm && tabElm.href) {
            this.router.navigateByUrl(tabElm.href);
        }
    }
}
TabsDelegate.decorators = [
    { type: Directive, args: [{
                selector: 'ion-tabs'
            },] },
];
/** @nocollapse */
TabsDelegate.ctorParameters = () => [
    { type: Router, decorators: [{ type: Optional },] },
    { type: ElementRef, },
];
TabsDelegate.propDecorators = {
    "onTabbarClick": [{ type: HostListener, args: ['ionTabbarClick', ['$event'],] },],
};
