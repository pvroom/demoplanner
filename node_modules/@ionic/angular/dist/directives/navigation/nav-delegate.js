import { ComponentFactoryResolver, Directive, ElementRef, Injector } from '@angular/core';
import { AngularDelegate } from '../../providers/angular-delegate';
export class NavDelegate {
    constructor(ref, cfr, injector, angularDelegate) {
        ref.nativeElement.delegate = angularDelegate.create(cfr, injector);
    }
}
NavDelegate.decorators = [
    { type: Directive, args: [{
                selector: 'ion-nav',
            },] },
];
/** @nocollapse */
NavDelegate.ctorParameters = () => [
    { type: ElementRef, },
    { type: ComponentFactoryResolver, },
    { type: Injector, },
    { type: AngularDelegate, },
];
