"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const list_1 = require("../utils/list");
const list_2 = require("./utils/list");
const sdk_1 = require("./utils/sdk");
async function run(args) {
    process.stdout.write(await list(args));
}
exports.run = run;
async function list(args) {
    const sdk = await sdk_1.getSDK();
    const [devices, virtualDevices] = await Promise.all([
        list_2.getDeviceTargets(sdk),
        list_2.getVirtualTargets(sdk),
    ]);
    return list_1.format(args, devices, virtualDevices);
}
exports.list = list;
